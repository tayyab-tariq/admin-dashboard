import {
    GridColumnMenuContainer,
  } from "@mui/x-data-grid";
  
const DataGridCustomColumnMenu = (props) => {
  const { hideMeny, currentColumn, open } = props;
  
    return (
    <GridColumnMenuContainer>
    </GridColumnMenuContainer>
  )
}

export default DataGridCustomColumnMenu